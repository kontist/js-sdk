name: Publish on tag

on:
  push:
    branches: [public-acl]
    tags:
      - v*

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: 12
          registry-url: "https://registry.npmjs.org"
      - name: npm install, build and test
        run: |
          npm ci
          npm run build --if-present
          npm audit --audit-level=moderate
          npm test
      - name: setup git
        run: |
          git config --global user.email "developer@kontist.com"
          git config --global user.name "GitHub"
      # - name: npm publish
      #   run: npm publish
      #   env:
      #     NODE_AUTH_TOKEN: ${{ secrets.NPM_AUTH_TOKEN }}
      - name: cdn upload
        run: aws s3 cp ./dist/bundle.js s3://${AWS_CDN_BUCKET}/sdk.min.js
        env:
          AWS_CDN_BUCKET: ${{ secrets.AWS_CDN_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets. AWS_SECRET_ACCESS_KEY }}
          AWS_EC2_METADATA_DISABLED: true
      - name: add public-read to sdk.min.js
        run: aws s3api put-object-acl --bucket ${AWS_CDN_BUCKET} --key sdk.min.js --acl public-read
        env:
          AWS_CDN_BUCKET: ${{ secrets.AWS_CDN_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets. AWS_SECRET_ACCESS_KEY }}
          AWS_EC2_METADATA_DISABLED: true
