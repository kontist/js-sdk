overwrite: true
schema: "http://localhost:3000/api/graphql"
documents: null
generates:
  lib/graphql/schema.ts:
    plugins:
      - typescript
  lib/graphql/schema.flow.js:
    plugins:
      - flow

